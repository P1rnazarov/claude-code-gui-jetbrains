/**
 * Application-wide constants.
 *
 * Single source of truth for the app name. Other literal occurrences of
 * 'Claude Code' (e.g. useDocumentTitle.ts) will be migrated in a separate
 * cleanup PR (see plan 016, Phase 2).
 */
export const APP_NAME = 'Claude Code';
